Smart SSC Question Book - PWA

IMPORTANT: Chrome's Install/Add to Home screen works only when this app is opened from an HTTPS web address (or localhost during development). Opening index.html directly as content:// will NOT show Install.

Upload the whole folder to any HTTPS static hosting service. Then open the resulting HTTPS link in Chrome and choose the browser menu -> Install app / Add to Home screen.
